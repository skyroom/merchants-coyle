const deliveryList = state => state.deliveryList || {};
const deliveryDetail = state => state.deliveryDetail || {};
const deliveryKey = state => state.deliveryKey || {};
const deliveryDetailKey = state => state.deliveryDetailKey || {};
export default {
    deliveryList,
    deliveryDetail,
    deliveryKey,
    deliveryDetailKey
};
