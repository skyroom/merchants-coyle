const purchaseList = state => state.purchaseList || {};
const purchaseDetail = state => state.purchaseDetail || {};
const purchaseKey = state => state.purchaseKey || {};
const purchaseDetailKey = state => state.purchaseDetailKey || {};
export default {
    purchaseList,
    purchaseDetail,
    purchaseKey,
    purchaseDetailKey
};
