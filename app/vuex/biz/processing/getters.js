const processList = state => state.processList || {};
const processDetail = state => state.processDetail || {};
const processListKey = state => state.processListKey || {};
const processDetailKey = state => state.processDetailKey || {};
export default {
    processList,
    processDetail,
    processListKey,
    processDetailKey,
};
